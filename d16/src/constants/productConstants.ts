// Allowed product categories — used for validation and type checking.

import { ProductCategory } from '../types';

export const ALLOWED_CATEGORIES: ProductCategory[] = [
  'electronics',
  'clothing',
  'food',
  'books',
  'toys',
];
